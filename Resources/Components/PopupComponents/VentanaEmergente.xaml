<?xml version="1.0" encoding="utf-8" ?>
<mopup:PopupPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ANTU.Resources.Components.PopupComponents.VentanaEmergente"
             xmlns:mopup="clr-namespace:Mopups.Pages;assembly=Mopups"
             BackgroundColor="#80000000"
             CloseWhenBackgroundIsClicked="False">

    <VerticalStackLayout
        HorizontalOptions="Center"
        VerticalOptions="Center">
        <Border
                BackgroundColor="#FFF"
                StrokeThickness="0"
                Padding="20">
            <StackLayout>
                <!-- <ActivityIndicator  -->
                <!--     x:Name="LoadingElement" -->
                <!--     IsRunning="True" Color="Orange"></ActivityIndicator> -->

                <Image
                    WidthRequest="50"
                    Source="{Binding Source}"/>
                <Label 
                    Text="{Binding Titulo}"
                    FontFamily="SegoeUIBold"
                    FontSize="20"
                    HorizontalTextAlignment="Center"
                    TextColor="#000"/>
                <Label 
                    Text="{Binding Contenido}"
                    FontFamily="SegoeUILight"
                    FontSize="15"
                    HorizontalTextAlignment="Center"
                    TextColor="#000"/>
                <Button
                    Margin="0,10,0,0"
                    FontFamily="SegoeUILight"
                    FontAttributes="Bold"
                    BackgroundColor="Orange"
                    x:Name="Button"
                    Clicked="Button_Clicked"
                    IsVisible="{Binding ShowButton}"
                    IsEnabled="{Binding ShowButton}"
                    Text="Entendido"/>
            </StackLayout>
        </Border>
    </VerticalStackLayout>
</mopup:PopupPage>