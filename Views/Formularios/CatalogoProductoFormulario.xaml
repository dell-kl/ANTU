<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:dataForm="clr-namespace:Syncfusion.Maui.DataForm;assembly=Syncfusion.Maui.DataForm"
             x:Class="ANTU.Views.Formularios.CatalogoProductoFormulario"
             xmlns:viewModel="clr-namespace:ANTU.ViewModel"
             Title="CatalogoFormulario"
             BackgroundColor="#D9D6D2"
             x:Name="PageCatalogoProductoFormulario"
             x:DataType="viewModel:CatalogoProductoFormularioViewModel">

    <Shell.TabBarIsVisible>False</Shell.TabBarIsVisible>

    <Shell.TitleView>
        <FlexLayout 
            JustifyContent="SpaceBetween"
            AlignItems="Center">
            <StackLayout>
                <Label 
                    FontFamily="SegoeUIBold"
                    FontSize="14"
                    TextTransform="Uppercase"
                    Text="Formulario Producto"/>
                <Label 
                    Margin="0,-5,0,0"
                    FontSize="13"
                    FontFamily="SegoeUILight"
                    Text="Registra producto de fabricacion"/>
            </StackLayout>

            <Border
                Margin="0,0,5,0"
                Stroke="#4CFFFFFF"
                StrokeShape="RoundRectangle 5,5,5,5"
                BackgroundColor="#4CFFFFFF">
                <Label 
                    FontSize="26"
                    Text="📋"/>
            </Border>
        </FlexLayout>
    </Shell.TitleView>

    <ScrollView>
        <Border 
            Margin="10"
            Stroke="LightGray"
            StrokeShape="RoundRectangle 5,5,5,5"
            BackgroundColor="#FFF"
            Grid.Column="0">
            <VerticalStackLayout 
                Margin="10,10,10,0">
                <Label
                        HorizontalTextAlignment="Start"
                        TextColor="#000"
                        FontFamily="SegoeUIBold"
                        FontSize="20"
                        Text="📝 Nuevo Producto"/>
                <Label 
                        HorizontalTextAlignment="Start"
                        TextColor="#8C3623"
                        FontFamily="SegoeUILight"
                        FontSize="15"
                        Text="Completa todos los datos referente al producto que fabricas"/>


                <VerticalStackLayout x:Name="EntradaFormularioImagenes"/>

                <VerticalStackLayout x:Name="StackLayoutCatalogoProductoFormulario"/>

            </VerticalStackLayout>
        </Border>
    </ScrollView>
    
</ContentPage>